import React, { Component, PropTypes } from 'react';

class Header extends Component {
  render() {
    return (
      <div />
    );
  }
}

Header.defaultProps = {};
Header.propTypes = {};

export default Header;
